{
  "name": "rewrite_constant_score_blended",
  "index": "cc-wet",
  "description": "Prefix query pinned to the 'constant_score_blended' rewrite. The 8.x default: blends a boolean rewrite for few expansions with a bitset for many.",
  "bindings": {
    "Prefix": {
      "source": "file",
      "path": "./assets/query/terms/prefix-3.txt"
    }
  },
  "template": {
    "_source": false,
    "query": {
      "prefix": {
        "content": {
          "value": "{{.Prefix}}",
          "rewrite": "constant_score_blended"
        }
      }
    }
  }
}
