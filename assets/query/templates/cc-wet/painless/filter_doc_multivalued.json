{
  "name": "filter_doc_multivalued",
  "index": "cc-wet",
  "description": "Iterates every value of a multi-valued keyword field. identified_content_language holds one entry per detected language, so this scales with the document rather than being constant.",
  "bindings": {
    "Day": {
      "source": "file",
      "path": "./assets/query/terms/date-values.txt"
    }
  },
  "template": {
    "_source": false,
    "size": 0,
    "track_total_hits": true,
    "query": {
      "bool": {
        "filter": [
          {
            "range": {
              "date": {
                "gte": "{{.Day}}||/d",
                "lt": "{{.Day}}||/d+1d"
              }
            }
          },
          {
            "script": {
              "script": {
                "source": "int n = 0; for (def s : doc['identified_content_language']) { n += s.length(); } return n >= 0;"
              }
            }
          }
        ]
      }
    }
  }
}
