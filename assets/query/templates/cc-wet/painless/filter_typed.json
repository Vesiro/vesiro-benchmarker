{
  "name": "filter_typed",
  "index": "cc-wet",
  "description": "The typed counterpart to filter_def_dynamic: identical arithmetic with static types, so the compiler can bind the operations directly.",
  "bindings": {
    "Day": {
      "source": "file",
      "path": "./assets/query/terms/date-values.txt"
    }
  },
  "template": {
    "_source": false,
    "size": 0,
    "track_total_hits": true,
    "query": {
      "bool": {
        "filter": [
          {
            "range": {
              "date": {
                "gte": "{{.Day}}||/d",
                "lt": "{{.Day}}||/d+1d"
              }
            }
          },
          {
            "script": {
              "script": {
                "source": "long a = doc['content_length'].value; long b = a * 2; long c = b + 1; return c >= 0;"
              }
            }
          }
        ]
      }
    }
  }
}
