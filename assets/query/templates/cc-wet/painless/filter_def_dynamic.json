{
  "name": "filter_def_dynamic",
  "index": "cc-wet",
  "description": "The same work as filter_typed but through def, so every operation resolves dynamically at runtime. The pair prices Painless's dynamic dispatch.",
  "bindings": {
    "Day": {
      "source": "file",
      "path": "./assets/query/terms/date-values.txt"
    }
  },
  "template": {
    "_source": false,
    "size": 0,
    "track_total_hits": true,
    "query": {
      "bool": {
        "filter": [
          {
            "range": {
              "date": {
                "gte": "{{.Day}}||/d",
                "lt": "{{.Day}}||/d+1d"
              }
            }
          },
          {
            "script": {
              "script": {
                "source": "def a = doc['content_length'].value; def b = a * 2; def c = b + 1; return c >= 0;"
              }
            }
          }
        ]
      }
    }
  }
}
