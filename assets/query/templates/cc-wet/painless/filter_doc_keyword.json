{
  "name": "filter_doc_keyword",
  "index": "cc-wet",
  "description": "One keyword doc-values read, which resolves a global ordinal rather than reading a packed number - usually dearer than the numeric read.",
  "bindings": {
    "Day": {
      "source": "file",
      "path": "./assets/query/terms/date-values.txt"
    }
  },
  "template": {
    "_source": false,
    "size": 0,
    "track_total_hits": true,
    "query": {
      "bool": {
        "filter": [
          {
            "range": {
              "date": {
                "gte": "{{.Day}}||/d",
                "lt": "{{.Day}}||/d+1d"
              }
            }
          },
          {
            "script": {
              "script": {
                "source": "def v = doc['identified_content_language']; return v.size() == 0 || v.value.length() >= 0;"
              }
            }
          }
        ]
      }
    }
  }
}
