{
  "name": "filter_lambda",
  "index": "cc-wet",
  "description": "A stream pipeline with two lambdas. Lambdas compile to invokedynamic call sites, so the first execution is dear and later ones should not be.",
  "bindings": {
    "Day": {
      "source": "file",
      "path": "./assets/query/terms/date-values.txt"
    }
  },
  "template": {
    "_source": false,
    "size": 0,
    "track_total_hits": true,
    "query": {
      "bool": {
        "filter": [
          {
            "range": {
              "date": {
                "gte": "{{.Day}}||/d",
                "lt": "{{.Day}}||/d+1d"
              }
            }
          },
          {
            "script": {
              "script": {
                "source": "List l = [1, 2, 3, 4, 5]; return l.stream().map(x -> x * 2).filter(x -> x > 2).count() >= 0;"
              }
            }
          }
        ]
      }
    }
  }
}
